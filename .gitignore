.DS_Store
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.ruff_cache/
*.egg-info/
dist/
build/
.env*
credentials*
# cached upstream metadata, not source
data/cache/

# wrangler's local cache. It holds the Cloudflare account id and the account
# name, which is an email address — and this repository is public.
.wrangler/
